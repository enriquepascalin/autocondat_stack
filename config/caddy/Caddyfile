:80, :443
root * /app/public
encode zstd gzip
file_server

# ↓ FrankenPHP worker – front-controller pattern
php_fastcgi unix//run/php/php.sock {
    env APP_ENV ${APP_ENV}
}

# Force HTTPS in prod (leave commented in local dev)
# @http  method GET
# redir @http https://{host}{uri}