{
  "users": [{
    "name": "${RABBITMQ_USER}",
    "password_hash": "",
    "hashing_algorithm": "rabbit_password_hashing_sha256",
    "tags": "administrator"
  }],
  "vhosts": [{ "name": "/" }],
  "permissions": [{
    "user": "${RABBITMQ_USER}",
    "vhost": "/",
    "configure": ".*",
    "write": ".*",
    "read": ".*"
  }]
}